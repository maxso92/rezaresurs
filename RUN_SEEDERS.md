# Запуск сидера для создания администратора

## Автоматическое создание админа

Сидер `AdminUserSeeder` уже настроен и будет запускаться автоматически при выполнении `php artisan db:seed`.

---

## Данные администратора по умолчанию

После запуска сидера будет создан администратор:

- **Email**: `admin@admin.com`
- **Пароль**: `password`
- **Имя**: `Admin`
- **Права**: Администратор (`is_admin = true`)

**ВАЖНО:** После первого входа обязательно измените пароль!

---

## Варианты запуска сидера

### Вариант 1: Запуск всех сидеров (РЕКОМЕНДУЕТСЯ)

Этот вариант запустит `DatabaseSeeder`, который автоматически вызовет `AdminUserSeeder`:

```bash
cd /var/www/rezaresurs_r_usr/data/www/rezaresurs.ru
PHP_PATH="/opt/alt/php83/usr/bin/php"

# Запуск всех сидеров
$PHP_PATH artisan db:seed
```

### Вариант 2: Запуск только сидера администратора

Если нужно запустить только сидер админа:

```bash
cd /var/www/rezaresurs_r_usr/data/www/rezaresurs.ru
PHP_PATH="/opt/alt/php83/usr/bin/php"

# Запуск только AdminUserSeeder
$PHP_PATH artisan db:seed --class=AdminUserSeeder
```

### Вариант 3: Миграции + Сидеры одной командой

Запустить миграции и сидеры одновременно:

```bash
cd /var/www/rezaresurs_r_usr/data/www/rezaresurs.ru
PHP_PATH="/opt/alt/php83/usr/bin/php"

# Миграции + Сидеры
$PHP_PATH artisan migrate --seed --force
```

---

## Добавление в инструкцию по развертыванию

После запуска миграций добавьте команду для сидеров:

```bash
# После команды:
$PHP_PATH artisan migrate --force

# Добавьте:
$PHP_PATH artisan db:seed

# Или объедините:
$PHP_PATH artisan migrate --seed --force
```

---

## Изменение данных администратора

Если хотите изменить email или пароль админа, отредактируйте файл:

**Файл:** `database/seeders/AdminUserSeeder.php`

```php
User::create([
    'name' => 'Ваше Имя',
    'email' => 'your-email@example.com',
    'password' => Hash::make('ВашПароль'),
    'is_admin' => true,
    'status' => 'active',
]);
```

После изменения нужно:
1. Удалить существующего админа (если уже создан)
2. Запустить сидер заново: `php artisan db:seed --class=AdminUserSeeder`

---

## Проверка создания администратора

После запуска сидера проверьте:

```bash
# Через tinker
$PHP_PATH artisan tinker

# В tinker выполните:
User::where('is_admin', true)->first();

# Должен вернуть объект пользователя с email admin@admin.com
exit
```

Или через базу данных:

```bash
# Для SQLite
sqlite3 database/database.sqlite "SELECT id, name, email, is_admin FROM users WHERE is_admin = 1;"

# Или через MySQL
mysql -u ваш_пользователь -p ваша_база -e "SELECT id, name, email, is_admin FROM users WHERE is_admin = 1;"
```

---

## Полная последовательность развертывания с сидером

```bash
cd /var/www/rezaresurs_r_usr/data/www/rezaresurs.ru
PHP_PATH="/opt/alt/php83/usr/bin/php"

# 1. Миграции
$PHP_PATH artisan migrate --force

# 2. Сидеры (создание админа)
$PHP_PATH artisan db:seed

# Или одной командой:
$PHP_PATH artisan migrate --seed --force
```

---

## Безопасность

**ОБЯЗАТЕЛЬНО после первого входа:**

1. Измените пароль администратора через интерфейс приложения
2. Или через tinker:
   ```bash
   $PHP_PATH artisan tinker
   ```
   ```php
   $admin = User::where('email', 'admin@admin.com')->first();
   $admin->password = Hash::make('НовыйНадежныйПароль');
   $admin->save();
   exit
   ```

3. Рассмотрите возможность удаления дефолтного админа и создания нового с уникальными данными

---

## Удаление и пересоздание админа

Если нужно пересоздать админа:

```bash
# Через tinker
$PHP_PATH artisan tinker
```

```php
// Удалите старого админа
User::where('email', 'admin@admin.com')->delete();

// Выйдите из tinker
exit
```

Затем запустите сидер заново:

```bash
$PHP_PATH artisan db:seed --class=AdminUserSeeder
```

---

## Готово!

После выполнения `php artisan db:seed` администратор будет создан, и вы сможете войти в систему с данными:
- Email: `admin@admin.com`
- Пароль: `password`

Не забудьте изменить пароль после первого входа! 🔐

